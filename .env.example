# --- Baserow Configuration ---
# The version of Baserow to deploy.
BASEROW_VERSION=1.34.2

# YOUR DOMAIN (Critical for Traefik routing).
# Do not include http:// or https://. Just the domain.
# Example: baserow.mydomain.com
BASEROW_DOMAIN=baserow.example.com

# --- Database (PostgreSQL) ---
DATABASE_USER=baserow
DATABASE_NAME=baserow
# GENERATE A STRONG PASSWORD HERE
DATABASE_PASSWORD=change_this_to_a_secure_password

# --- Redis ---
# GENERATE A STRONG PASSWORD HERE
REDIS_PASSWORD=change_this_to_a_secure_password

# --- Baserow Security ---
# You must generate random 64-character strings for these keys.
# You can use `openssl rand -hex 32` in a terminal to generate them.
SECRET_KEY=change_this_to_a_long_random_secret_string
BASEROW_JWT_SIGNING_KEY=change_this_to_a_long_random_jwt_key

# --- Emails (SMTP) ---
# Required for inviting users and password resets.
EMAIL_SMTP_HOST=smtp.example.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USE_TLS=true
EMAIL_SMTP_USER=user@example.com
EMAIL_SMTP_PASSWORD=password
FROM_EMAIL=no-reply@example.com

# --- Object Storage S3 (Optional) ---
# Leave empty if you want to store files locally on the server disk.
# If using MinIO (hosted on Coolify), use the internal URL.
BASEROW_S3_ACCESS_KEY_ID=
BASEROW_S3_SECRET_ACCESS_KEY=
BASEROW_S3_BUCKET=
BASEROW_S3_REGION=
BASEROW_S3_ENDPOINT_URL=
BASEROW_S3_USE_PATH_STYLE_ENDPOINT=true

# --- Performance & Debug ---
# Number of workers. Increase based on your server's CPU cores.
BASEROW_AMOUNT_OF_WORKERS=2
BASEROW_BACKEND_DEBUG=false

# --- Coolify Specifics ---
# The name of the external network where Traefik runs.
# Default is usually 'coolify'.
COOLIFY_PROXY_NETWORK=coolify
